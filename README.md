# byte-forms

BYTE College Society member data collection app. React + Vite + Tailwind CSS with shadcn-style UI components, Dockerized for deployment.

## Run
`npm install && npm run dev`

## Docker
`docker compose up --build` then visit http://localhost:8083

Postgres is included as the persistence service scaffold; connect it to your API layer for production submissions.
