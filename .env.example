# Local development defaults
DATABASE_URL=postgres://byte:byte_local_dev@localhost:5432/byte_forms
PORT=3001
NODE_ENV=development
UPLOAD_DIR=./uploads

# Required to create the first admin (never commit real credentials)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=replace-with-a-long-random-password
